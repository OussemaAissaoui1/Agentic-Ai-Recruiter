# tests/

```
tests/
├── unit/         # per-agent unit tests (placeholder)
└── integration/
    └── test_unified_app.py    # smoke test for /api/health + agent cards + dispatch
```

Run with `pytest -q tests/` from the ai-recruiter root.
